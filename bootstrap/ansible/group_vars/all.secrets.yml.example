---
# Local-only secrets for Ansible.
#
# 1) Copy this file to `bootstrap/ansible/group_vars/all.secrets.yml`
# 2) Fill in real values
# 3) Keep it untracked (it is gitignored)
#
# Google OAuth
google_client_id: "YOUR_GOOGLE_CLIENT_ID"
google_client_secret: "YOUR_GOOGLE_CLIENT_SECRET"

# Auth.js / NextAuth
nextauth_secret: "PASTE_A_RANDOM_BASE64_STRING"

# Optional (only required if overriding)
# nextauth_url: "https://keithwilliams.org"
